@model IEnumerable<TreinoDoRamon.Models.Exercicio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Musculo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imagem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tempomax)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Musculo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imagem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tempomax)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idexercicios">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idexercicios">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idexercicios">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
